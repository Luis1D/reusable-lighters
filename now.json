{
    "version": 2,
    "name": "reusablelighters",
    "builds": [{ "src": "reusablelighters/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "reusablelighters/$1" }
    ]
}